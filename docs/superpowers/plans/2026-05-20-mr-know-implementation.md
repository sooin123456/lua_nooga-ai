# Mr Know Toss Mini App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the "미스터 노우" Toss WebView mini app MVP with four input flows, OCR-assisted screenshot handling, rule-based judgment, safety handling, and result display.

**Architecture:** Start from the official Apps in Toss WebView scaffold, then replace the sample app with a small React state-machine app. Keep domain logic in pure TypeScript modules (`analyzer`, `safety`, `ocr`, `audio`) so tests can verify behavior without a browser. UI components consume those modules through narrow adapters and keep all user-provided content in memory only.

**Tech Stack:** `create-ait-app@0.0.2`, `@apps-in-toss/web-framework@2.6.0`, React 18, TypeScript 5.7, Toss TDS Mobile, Vite, Vitest, React Testing Library, `tesseract.js`, `lucide-react`.

---

## Pre-Flight Notes

- Current repository already contains design docs and git history, so do not scaffold directly into the repository root.
- Create the Apps in Toss scaffold in a temporary directory, then copy generated files into `/Users/sooin/Documents/Lua_testproject` while preserving `docs/superpowers`.
- The scaffold install warns that `@apps-in-toss/ait-format@1.0.0` requires Node `>=24`. The current local Node is `v20.19.4`; record this in the README and run available checks on the current runtime. If `npm run build` fails only because of that engine requirement, capture the exact failure.
- Do not copy `Lua_template` into this project. Use it only as an organizational reference.

## File Structure

Create or modify these files:

- Create: `AGENTS.md` - Apps in Toss/TDS guidance generated by scaffold.
- Create: `README.md` - local setup, Node version note, verification commands, privacy note.
- Create: `docs/skills/apps-in-toss.md` - generated Apps in Toss skill notes.
- Create: `docs/skills/tds-mobile.md` - generated TDS Mobile skill notes.
- Create: `eslint.config.js` - generated ESLint config.
- Create: `granite.config.ts` - Apps in Toss dev/build config.
- Create: `index.html` - Vite HTML shell.
- Create: `package.json` - app scripts and dependencies.
- Create: `package-lock.json` - npm lockfile.
- Create: `public/appsintoss-logo.png` - generated asset.
- Create: `src/main.tsx` - React entry.
- Create: `src/App.tsx` - app-level state machine and screen switching.
- Create: `src/App.css` - app layout and visual styling.
- Create: `src/index.css` - global reset and theme tokens.
- Create: `src/vite-env.d.ts` - generated Vite types.
- Create: `src/features/input/inputMethods.ts` - four home input cards and labels.
- Create: `src/features/input/InputHome.tsx` - first screen.
- Create: `src/features/input/TextReview.tsx` - text confirmation/editing screen.
- Create: `src/features/result/ResultScreen.tsx` - judgment result screen.
- Create: `src/features/analyzer/types.ts` - shared judgment types.
- Create: `src/features/analyzer/ruleBasedAnalyzer.ts` - deterministic MVP analyzer.
- Create: `src/features/analyzer/ruleBasedAnalyzer.test.ts` - analyzer unit tests.
- Create: `src/features/safety/safety.ts` - urgent/caution signal detection.
- Create: `src/features/safety/safety.test.ts` - safety unit tests.
- Create: `src/features/ocr/ocrAdapter.ts` - browser OCR adapter using lazy-loaded `tesseract.js`.
- Create: `src/features/ocr/ocrAdapter.test.ts` - OCR fallback/unit tests.
- Create: `src/features/audio/audioAdapter.ts` - recording/file transcription adapter interface and manual fallback.
- Create: `src/features/audio/audioAdapter.test.ts` - audio adapter tests.
- Create: `src/shared/ui/MrKnowMascot.tsx` - cute judge character rendered with CSS/HTML.
- Create: `src/test/setup.ts` - Vitest DOM setup.
- Create: `tsconfig.app.json`, `tsconfig.json`, `tsconfig.node.json`, `vite.config.ts` - generated scaffold plus Vitest config additions.

---

### Task 1: Scaffold Apps in Toss Project

**Files:**
- Create: all scaffold files listed in File Structure
- Modify: `package.json`
- Modify: `vite.config.ts`
- Create: `src/test/setup.ts`

- [ ] **Step 1: Generate scaffold in `/tmp`**

Run:

```bash
rm -rf /tmp/mr-know-scaffold
npx create-ait-app@latest mr-know-scaffold --inline --pm npm --tds --skills --ai codex
```

Expected: command ends with `✅ 프로젝트가 성공적으로 생성되었습니다!`.

- [ ] **Step 2: Copy scaffold files into the repository**

Run:

```bash
rsync -a \
  --exclude '.git' \
  --exclude 'node_modules' \
  /tmp/mr-know-scaffold/ \
  /Users/sooin/Documents/Lua_testproject/
```

Expected: repository root now has `package.json`, `src/App.tsx`, `granite.config.ts`, and existing `docs/superpowers` is still present.

- [ ] **Step 3: Install test and feature dependencies**

Run:

```bash
npm install tesseract.js lucide-react
npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom
```

Expected: `package.json` contains runtime dependencies `tesseract.js` and `lucide-react`, and dev dependencies for Vitest and Testing Library.

- [ ] **Step 4: Update `package.json` scripts**

Replace the `scripts` object in `package.json` with:

```json
{
  "dev": "granite dev",
  "build": "ait build",
  "deploy": "ait deploy",
  "lint": "eslint .",
  "format": "prettier --write .",
  "test": "vitest run",
  "test:watch": "vitest"
}
```

- [ ] **Step 5: Update `vite.config.ts` for Vitest**

Replace `vite.config.ts` with:

```ts
import react from "@vitejs/plugin-react";
import { defineConfig } from "vitest/config";

export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    passWithNoTests: true,
    setupFiles: "./src/test/setup.ts",
  },
});
```

- [ ] **Step 6: Create `src/test/setup.ts`**

```ts
import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 7: Run scaffold verification**

Run:

```bash
npm run lint
npm test
```

Expected: `npm run lint` passes. `npm test` passes with no test files or reports no tests only if Vitest exits successfully.

- [ ] **Step 8: Commit scaffold**

```bash
git add AGENTS.md README.md docs/skills eslint.config.js granite.config.ts index.html package.json package-lock.json public src tsconfig*.json vite.config.ts
git commit -m "chore: scaffold Apps in Toss app"
```

---

### Task 2: Add Domain Types, Safety Detection, and Rule-Based Analyzer

**Files:**
- Create: `src/features/analyzer/types.ts`
- Create: `src/features/analyzer/ruleBasedAnalyzer.ts`
- Create: `src/features/analyzer/ruleBasedAnalyzer.test.ts`
- Create: `src/features/safety/safety.ts`
- Create: `src/features/safety/safety.test.ts`

- [ ] **Step 1: Write analyzer and safety tests**

Create `src/features/analyzer/ruleBasedAnalyzer.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { analyzeWithRules } from "./ruleBasedAnalyzer";

describe("analyzeWithRules", () => {
  it("returns a complete judgment with percentages that add up to 100", async () => {
    const result = await analyzeWithRules({
      text: "A: 미안한데 내가 말이 좀 셌어.\nB: 너는 항상 이기적이야. 됐고 내 말만 들어.",
    });

    expect(result.partyAPercent + result.partyBPercent).toBe(100);
    expect(result.reasons).toHaveLength(3);
    expect(result.advice.length).toBeGreaterThan(0);
    expect(result.verdict.length).toBeGreaterThan(0);
    expect(result.safetyLevel).toBe("normal");
  });

  it("keeps party percentages within readable bounds", async () => {
    const result = await analyzeWithRules({
      text: "A: 다 네 탓이야. 사과 안 하면 끝이야.\nB: 내가 늦게 말한 건 미안해. 다시 이야기하자.",
    });

    expect(result.partyAPercent).toBeGreaterThanOrEqual(5);
    expect(result.partyAPercent).toBeLessThanOrEqual(95);
    expect(result.partyBPercent).toBeGreaterThanOrEqual(5);
    expect(result.partyBPercent).toBeLessThanOrEqual(95);
  });

  it("prioritizes urgent safety signals over normal judgment", async () => {
    const result = await analyzeWithRules({
      text: "A: 죽여버린다고 했고 집 앞에서 기다리겠다고 했어.\nB: 너무 무서워.",
    });

    expect(result.safetyLevel).toBe("urgent");
    expect(result.verdict).toContain("안전");
  });
});
```

Create `src/features/safety/safety.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { detectSafetyLevel } from "./safety";

describe("detectSafetyLevel", () => {
  it("marks violence and stalking language as urgent", () => {
    expect(detectSafetyLevel("때리겠다 죽여버리겠다 집 앞에서 기다린다")).toBe("urgent");
  });

  it("marks coercive control language as caution", () => {
    expect(detectSafetyLevel("휴대폰 검사하고 누구 만나는지 매번 허락받으라고 했어")).toBe("caution");
  });

  it("returns normal for ordinary conflict", () => {
    expect(detectSafetyLevel("서로 말투 때문에 서운해서 다퉜어")).toBe("normal");
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run:

```bash
npm test -- src/features/analyzer/ruleBasedAnalyzer.test.ts src/features/safety/safety.test.ts
```

Expected: fails because modules do not exist.

- [ ] **Step 3: Create analyzer types**

Create `src/features/analyzer/types.ts`:

```ts
export type SafetyLevel = "normal" | "caution" | "urgent";

export type JudgmentResult = {
  verdict: string;
  partyAPercent: number;
  partyBPercent: number;
  reasons: [string, string, string];
  advice: string;
  safetyLevel: SafetyLevel;
};

export type AnalyzeInput = {
  text: string;
  partyALabel?: string;
  partyBLabel?: string;
};

export type Analyzer = {
  analyze(input: AnalyzeInput): Promise<JudgmentResult>;
};
```

- [ ] **Step 4: Create safety detection**

Create `src/features/safety/safety.ts`:

```ts
import type { SafetyLevel } from "../analyzer/types";

const urgentPatterns = [
  /죽여/,
  /때리/,
  /폭행/,
  /협박/,
  /집 앞/,
  /스토킹/,
  /몰래\s*촬영/,
  /불법\s*촬영/,
  /자해/,
  /죽고\s*싶/,
];

const cautionPatterns = [
  /휴대폰\s*검사/,
  /허락받/,
  /통제/,
  /감시/,
  /강요/,
  /협박하듯/,
  /미성년/,
];

export function detectSafetyLevel(text: string): SafetyLevel {
  const compactText = text.replace(/\s+/g, " ").trim();

  if (urgentPatterns.some((pattern) => pattern.test(compactText))) {
    return "urgent";
  }

  if (cautionPatterns.some((pattern) => pattern.test(compactText))) {
    return "caution";
  }

  return "normal";
}
```

- [ ] **Step 5: Create rule-based analyzer**

Create `src/features/analyzer/ruleBasedAnalyzer.ts`:

```ts
import { detectSafetyLevel } from "../safety/safety";
import type { AnalyzeInput, Analyzer, JudgmentResult } from "./types";

const partyAPattern = /(^|\n)\s*(A|나|남친|여친|상대1)\s*[:：]/gi;
const partyBPattern = /(^|\n)\s*(B|상대|남친|여친|상대2)\s*[:：]/gi;

const blamePatterns = [/항상/g, /절대/g, /네 탓/g, /너 때문/g, /이기적/g, /됐고/g, /내 말만/g];
const apologyPatterns = [/미안/g, /사과/g, /내가.*잘못/g, /말이.*셌/g, /다시.*이야기/g];
const dismissPatterns = [/그만해/g, /시끄러/g, /상관없/g, /알아서 해/g, /됐어/g];
const repairPatterns = [/다시/g, /같이/g, /해결/g, /이야기하/g, /정리하/g, /생각해보/g];

function countMatches(text: string, patterns: RegExp[]) {
  return patterns.reduce((count, pattern) => count + (text.match(pattern)?.length ?? 0), 0);
}

function splitPartyText(text: string) {
  const lines = text.split(/\n+/);
  const aLines: string[] = [];
  const bLines: string[] = [];
  const sharedLines: string[] = [];

  for (const line of lines) {
    if (/^\s*(A|나|상대1)\s*[:：]/i.test(line)) {
      aLines.push(line);
    } else if (/^\s*(B|상대|상대2)\s*[:：]/i.test(line)) {
      bLines.push(line);
    } else {
      sharedLines.push(line);
    }
  }

  return {
    aText: aLines.length > 0 ? aLines.join("\n") : sharedLines.join("\n"),
    bText: bLines.length > 0 ? bLines.join("\n") : sharedLines.join("\n"),
  };
}

function scoreParty(text: string) {
  const blame = countMatches(text, blamePatterns);
  const dismiss = countMatches(text, dismissPatterns);
  const apology = countMatches(text, apologyPatterns);
  const repair = countMatches(text, repairPatterns);
  return Math.max(1, 12 + blame * 16 + dismiss * 12 - apology * 10 - repair * 5);
}

function clampPercent(value: number) {
  return Math.min(95, Math.max(5, value));
}

export async function analyzeWithRules(input: AnalyzeInput): Promise<JudgmentResult> {
  const text = input.text.trim();
  const safetyLevel = detectSafetyLevel(text);

  if (safetyLevel === "urgent") {
    return {
      verdict: "지금은 판정보다 안전이 먼저예요",
      partyAPercent: 50,
      partyBPercent: 50,
      reasons: [
        "폭력, 협박, 스토킹처럼 보일 수 있는 표현이 포함돼 있어요.",
        "이 상황은 재미용 승패 판정보다 안전 확보가 우선이에요.",
        "혼자 해결하려 하기보다 믿을 수 있는 사람이나 기관에 도움을 요청하는 편이 좋아요.",
      ],
      advice: "위협이 계속된다면 대화를 멈추고 안전한 장소와 도움을 먼저 확보하세요.",
      safetyLevel,
    };
  }

  const { aText, bText } = splitPartyText(text);
  const hasPartyMarkers = partyAPattern.test(text) || partyBPattern.test(text);
  partyAPattern.lastIndex = 0;
  partyBPattern.lastIndex = 0;

  const aScore = scoreParty(aText);
  const bScore = scoreParty(bText);
  const rawAPercent = hasPartyMarkers ? Math.round((aScore / (aScore + bScore)) * 100) : 55;
  const partyAPercent = clampPercent(rawAPercent);
  const partyBPercent = 100 - partyAPercent;
  const leadingParty = partyAPercent >= partyBPercent ? "A" : "B";
  const leadingPercent = Math.max(partyAPercent, partyBPercent);

  return {
    verdict:
      safetyLevel === "caution"
        ? "살짝 위험 신호가 보여요. 판정은 조심스럽게 볼게요"
        : `${leadingParty}가 ${leadingPercent}% 정도 더 선 넘었어요`,
    partyAPercent,
    partyBPercent,
    reasons: [
      "비난 표현과 단정적인 말투가 판정 점수에 크게 반영됐어요.",
      "사과, 인정, 해결 제안이 있는 쪽은 잘못 비율이 낮아졌어요.",
      "입력된 대화 기준이라 빠진 맥락이 있으면 결과가 달라질 수 있어요.",
    ],
    advice:
      leadingParty === "A"
        ? "A는 먼저 말투를 인정하고, B가 들은 감정을 한 문장으로 되짚어 주세요."
        : "B는 먼저 말투를 인정하고, A가 들은 감정을 한 문장으로 되짚어 주세요.",
    safetyLevel,
  };
}

export const ruleBasedAnalyzer: Analyzer = {
  analyze: analyzeWithRules,
};
```

- [ ] **Step 6: Run tests to verify they pass**

Run:

```bash
npm test -- src/features/analyzer/ruleBasedAnalyzer.test.ts src/features/safety/safety.test.ts
```

Expected: all tests pass.

- [ ] **Step 7: Commit domain logic**

```bash
git add src/features/analyzer src/features/safety
git commit -m "feat: add rule based judgment analyzer"
```

---

### Task 3: Build Home Input Screen and Mascot

**Files:**
- Create: `src/features/input/inputMethods.ts`
- Create: `src/features/input/InputHome.tsx`
- Create: `src/shared/ui/MrKnowMascot.tsx`
- Modify: `src/App.css`

- [ ] **Step 1: Write home screen test**

Create `src/features/input/InputHome.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import { InputHome } from "./InputHome";

describe("InputHome", () => {
  it("shows the four approved input cards", () => {
    render(<InputHome onSelect={vi.fn()} />);

    expect(screen.getByRole("button", { name: /카톡 싸움 붙여넣기/ })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: /증거 캡처 제출하기/ })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: /현장 녹음 시작/ })).toBeInTheDocument();
    expect(screen.getByRole("button", { name: /녹음 파일 불러오기/ })).toBeInTheDocument();
  });

  it("calls onSelect with the selected method", async () => {
    const onSelect = vi.fn();
    render(<InputHome onSelect={onSelect} />);

    await userEvent.click(screen.getByRole("button", { name: /증거 캡처 제출하기/ }));

    expect(onSelect).toHaveBeenCalledWith("screenshot");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/features/input/InputHome.test.tsx
```

Expected: fails because `InputHome` does not exist.

- [ ] **Step 3: Create input method data**

Create `src/features/input/inputMethods.ts`:

```ts
import { Camera, ClipboardPaste, Mic, Upload } from "lucide-react";
import type { ComponentType } from "react";

export type InputMethod = "text" | "screenshot" | "record" | "audio-file";

export type InputMethodCard = {
  id: InputMethod;
  title: string;
  description: string;
  Icon: ComponentType<{ size?: number; strokeWidth?: number }>;
};

export const inputMethods: InputMethodCard[] = [
  {
    id: "text",
    title: "카톡 싸움 붙여넣기",
    description: "복사한 대화나 상황 설명을 바로 맡겨요.",
    Icon: ClipboardPaste,
  },
  {
    id: "screenshot",
    title: "증거 캡처 제출하기",
    description: "캡처 속 글자를 자동으로 읽어볼게요.",
    Icon: Camera,
  },
  {
    id: "record",
    title: "현장 녹음 시작",
    description: "방금 벌어진 말싸움을 기록해요.",
    Icon: Mic,
  },
  {
    id: "audio-file",
    title: "녹음 파일 불러오기",
    description: "이미 저장한 음성 증거를 가져와요.",
    Icon: Upload,
  },
];
```

- [ ] **Step 4: Create mascot**

Create `src/shared/ui/MrKnowMascot.tsx`:

```tsx
export function MrKnowMascot() {
  return (
    <div className="mascot" aria-hidden="true">
      <div className="mascot__face">
        <span className="mascot__eye" />
        <span className="mascot__eye" />
        <span className="mascot__mouth" />
      </div>
      <div className="mascot__badge">KNOW</div>
    </div>
  );
}
```

- [ ] **Step 5: Create home screen**

Create `src/features/input/InputHome.tsx`:

```tsx
import { Top } from "@toss/tds-mobile";
import { MrKnowMascot } from "../../shared/ui/MrKnowMascot";
import { inputMethods, type InputMethod } from "./inputMethods";

type InputHomeProps = {
  onSelect(method: InputMethod): void;
};

export function InputHome({ onSelect }: InputHomeProps) {
  return (
    <main className="screen screen--home">
      <Top
        title={<Top.TitleParagraph size={26}>미스터 노우</Top.TitleParagraph>}
        subtitleBottom={
          <Top.SubtitleParagraph size={16}>
            싸움의 증거를 내면, 귀여운 심판이 퍼센티지로 판정해요.
          </Top.SubtitleParagraph>
        }
      />

      <section className="hero-panel" aria-label="미스터 노우 소개">
        <MrKnowMascot />
        <div>
          <p className="eyebrow">오늘의 판정 대기 중</p>
          <h1>누가 얼마나 선 넘었는지 볼까요?</h1>
        </div>
      </section>

      <section className="method-grid" aria-label="입력 방식 선택">
        {inputMethods.map(({ id, title, description, Icon }) => (
          <button className="method-card" key={id} type="button" onClick={() => onSelect(id)}>
            <span className="method-card__icon">
              <Icon size={22} strokeWidth={2.2} />
            </span>
            <span className="method-card__copy">
              <strong>{title}</strong>
              <span>{description}</span>
            </span>
          </button>
        ))}
      </section>
    </main>
  );
}
```

- [ ] **Step 6: Add styling for home and mascot**

Append to `src/App.css`:

```css
.screen {
  min-height: 100vh;
  padding: 20px 18px 32px;
  background: #f7f8fa;
  color: #191f28;
}

.screen--home {
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.hero-panel {
  display: grid;
  grid-template-columns: auto 1fr;
  gap: 16px;
  align-items: center;
  padding: 18px;
  border: 1px solid #e5e8eb;
  border-radius: 8px;
  background: #ffffff;
}

.hero-panel h1 {
  margin: 4px 0 0;
  font-size: 22px;
  line-height: 1.25;
  letter-spacing: 0;
}

.eyebrow {
  margin: 0;
  color: #4e5968;
  font-size: 13px;
  font-weight: 700;
}

.mascot {
  position: relative;
  width: 74px;
  height: 86px;
}

.mascot__face {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 10px;
  width: 74px;
  height: 74px;
  border: 3px solid #191f28;
  border-radius: 50%;
  background: #ffdf6e;
  box-shadow: 0 8px 0 #191f28;
}

.mascot__eye {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #191f28;
}

.mascot__mouth {
  position: absolute;
  bottom: 30px;
  left: 31px;
  width: 12px;
  height: 6px;
  border-bottom: 3px solid #191f28;
  border-radius: 0 0 12px 12px;
}

.mascot__badge {
  position: absolute;
  right: -8px;
  bottom: 3px;
  padding: 3px 6px;
  border: 2px solid #191f28;
  border-radius: 6px;
  background: #ffffff;
  font-size: 10px;
  font-weight: 800;
}

.method-grid {
  display: grid;
  gap: 10px;
}

.method-card {
  display: grid;
  grid-template-columns: 44px 1fr;
  gap: 12px;
  align-items: center;
  width: 100%;
  min-height: 82px;
  padding: 14px;
  border: 1px solid #e5e8eb;
  border-radius: 8px;
  background: #ffffff;
  color: #191f28;
  text-align: left;
}

.method-card__icon {
  display: grid;
  place-items: center;
  width: 44px;
  height: 44px;
  border-radius: 8px;
  background: #e8f3ff;
  color: #1b64da;
}

.method-card__copy {
  display: grid;
  gap: 4px;
}

.method-card__copy strong {
  font-size: 17px;
  line-height: 1.25;
}

.method-card__copy span {
  color: #6b7684;
  font-size: 14px;
  line-height: 1.35;
}
```

- [ ] **Step 7: Run test and lint**

Run:

```bash
npm test -- src/features/input/InputHome.test.tsx
npm run lint
```

Expected: both pass.

- [ ] **Step 8: Commit home screen**

```bash
git add src/features/input src/shared/ui src/App.css
git commit -m "feat: add Mr Know input home"
```

---

### Task 4: Add Text Review State Machine

**Files:**
- Modify: `src/App.tsx`
- Create: `src/features/input/TextReview.tsx`
- Create: `src/features/input/TextReview.test.tsx`
- Modify: `src/App.css`

- [ ] **Step 1: Write text review test**

Create `src/features/input/TextReview.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import { TextReview } from "./TextReview";

describe("TextReview", () => {
  it("requires text before analysis", async () => {
    const onAnalyze = vi.fn();
    render(<TextReview initialText="" sourceLabel="카톡 싸움 붙여넣기" onAnalyze={onAnalyze} onBack={vi.fn()} />);

    await userEvent.click(screen.getByRole("button", { name: "판정 받기" }));

    expect(onAnalyze).not.toHaveBeenCalled();
    expect(screen.getByText("판정할 내용을 먼저 입력해주세요.")).toBeInTheDocument();
  });

  it("submits edited text", async () => {
    const onAnalyze = vi.fn();
    render(<TextReview initialText="A: 미안해" sourceLabel="카톡 싸움 붙여넣기" onAnalyze={onAnalyze} onBack={vi.fn()} />);

    await userEvent.clear(screen.getByLabelText("분석할 대화 내용"));
    await userEvent.type(screen.getByLabelText("분석할 대화 내용"), "A: 미안해\nB: 네 탓이야");
    await userEvent.click(screen.getByRole("button", { name: "판정 받기" }));

    expect(onAnalyze).toHaveBeenCalledWith("A: 미안해\nB: 네 탓이야");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/features/input/TextReview.test.tsx
```

Expected: fails because `TextReview` does not exist.

- [ ] **Step 3: Create text review screen**

Create `src/features/input/TextReview.tsx`:

```tsx
import { Button, Top } from "@toss/tds-mobile";
import { useState } from "react";

type TextReviewProps = {
  initialText: string;
  sourceLabel: string;
  helperText?: string;
  onAnalyze(text: string): void;
  onBack(): void;
};

export function TextReview({ initialText, sourceLabel, helperText, onAnalyze, onBack }: TextReviewProps) {
  const [text, setText] = useState(initialText);
  const [error, setError] = useState("");

  function handleAnalyze() {
    const trimmedText = text.trim();
    if (trimmedText.length === 0) {
      setError("판정할 내용을 먼저 입력해주세요.");
      return;
    }

    setError("");
    onAnalyze(trimmedText);
  }

  return (
    <main className="screen screen--review">
      <Top
        title={<Top.TitleParagraph size={24}>내용 확인</Top.TitleParagraph>}
        subtitleBottom={
          <Top.SubtitleParagraph size={16}>
            {sourceLabel} 내용을 보고 미스터 노우가 판정할 수 있게 정리해주세요.
          </Top.SubtitleParagraph>
        }
      />

      {helperText ? <p className="notice">{helperText}</p> : null}

      <label className="text-review">
        <span>분석할 대화 내용</span>
        <textarea
          aria-label="분석할 대화 내용"
          value={text}
          onChange={(event) => setText(event.target.value)}
          placeholder={"A: 약속 시간 늦은 건 미안해.\nB: 너는 항상 이런 식이야."}
        />
      </label>

      {error ? <p className="form-error">{error}</p> : null}

      <div className="action-row">
        <Button variant="weak" onClick={onBack}>
          돌아가기
        </Button>
        <Button onClick={handleAnalyze}>판정 받기</Button>
      </div>
    </main>
  );
}
```

- [ ] **Step 4: Replace sample `src/App.tsx` with app state**

```tsx
import { useState } from "react";
import "./App.css";
import { analyzeWithRules } from "./features/analyzer/ruleBasedAnalyzer";
import type { JudgmentResult } from "./features/analyzer/types";
import { InputHome } from "./features/input/InputHome";
import type { InputMethod } from "./features/input/inputMethods";
import { inputMethods } from "./features/input/inputMethods";
import { TextReview } from "./features/input/TextReview";

type Screen =
  | { name: "home" }
  | { name: "review"; method: InputMethod; initialText: string; helperText?: string }
  | { name: "result"; result: JudgmentResult };

const starterText = "A: 약속 시간 늦은 건 미안해.\nB: 너는 항상 네 생각만 해. 됐고 내 말만 들어.";

function sourceLabelFor(method: InputMethod) {
  return inputMethods.find((item) => item.id === method)?.title ?? "입력 내용";
}

function App() {
  const [screen, setScreen] = useState<Screen>({ name: "home" });
  const [isAnalyzing, setIsAnalyzing] = useState(false);

  function handleSelect(method: InputMethod) {
    if (method === "text") {
      setScreen({ name: "review", method, initialText: starterText });
      return;
    }

    if (method === "screenshot") {
      setScreen({
        name: "review",
        method,
        initialText: "",
        helperText: "캡처 OCR은 다음 단계에서 자동 추출로 연결됩니다. 지금은 내용을 붙여넣어 판정 흐름을 확인할 수 있어요.",
      });
      return;
    }

    setScreen({
      name: "review",
      method,
      initialText: "",
      helperText: "녹음 내용은 분석 전에 텍스트로 확인해야 해요. 들은 내용을 붙여넣거나 직접 적어주세요.",
    });
  }

  async function handleAnalyze(text: string) {
    setIsAnalyzing(true);
    const result = await analyzeWithRules({ text });
    setIsAnalyzing(false);
    setScreen({ name: "result", result });
  }

  if (screen.name === "home") {
    return <InputHome onSelect={handleSelect} />;
  }

  if (screen.name === "review") {
    return (
      <TextReview
        initialText={screen.initialText}
        sourceLabel={sourceLabelFor(screen.method)}
        helperText={isAnalyzing ? "판정 중이에요." : screen.helperText}
        onAnalyze={handleAnalyze}
        onBack={() => setScreen({ name: "home" })}
      />
    );
  }

  return (
    <main className="screen">
      <pre>{JSON.stringify(screen.result, null, 2)}</pre>
      <button type="button" onClick={() => setScreen({ name: "home" })}>
        다시 판정하기
      </button>
    </main>
  );
}

export default App;
```

- [ ] **Step 5: Add review styling**

Append to `src/App.css`:

```css
.screen--review {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.notice {
  margin: 0;
  padding: 12px;
  border-radius: 8px;
  background: #fff7e6;
  color: #8b5e00;
  font-size: 14px;
  line-height: 1.45;
}

.text-review {
  display: grid;
  gap: 8px;
  color: #333d4b;
  font-size: 14px;
  font-weight: 700;
}

.text-review textarea {
  width: 100%;
  min-height: 260px;
  padding: 14px;
  border: 1px solid #d1d6db;
  border-radius: 8px;
  background: #ffffff;
  color: #191f28;
  font: inherit;
  font-weight: 500;
  line-height: 1.5;
  resize: vertical;
}

.form-error {
  margin: 0;
  color: #f04452;
  font-size: 14px;
  font-weight: 700;
}

.action-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 10px;
  margin-top: auto;
}
```

- [ ] **Step 6: Run tests and lint**

Run:

```bash
npm test -- src/features/input/TextReview.test.tsx src/features/input/InputHome.test.tsx
npm run lint
```

Expected: both commands pass.

- [ ] **Step 7: Commit review flow**

```bash
git add src/App.tsx src/App.css src/features/input/TextReview.tsx src/features/input/TextReview.test.tsx
git commit -m "feat: add text review flow"
```

---

### Task 5: Replace Debug Result With Final Result Screen

**Files:**
- Create: `src/features/result/ResultScreen.tsx`
- Create: `src/features/result/ResultScreen.test.tsx`
- Modify: `src/App.tsx`
- Modify: `src/App.css`

- [ ] **Step 1: Write result screen test**

Create `src/features/result/ResultScreen.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { describe, expect, it, vi } from "vitest";
import type { JudgmentResult } from "../analyzer/types";
import { ResultScreen } from "./ResultScreen";

const result: JudgmentResult = {
  verdict: "A가 68% 정도 더 선 넘었어요",
  partyAPercent: 68,
  partyBPercent: 32,
  reasons: ["비난 표현이 많았어요.", "사과가 늦었어요.", "해결 제안이 부족했어요."],
  advice: "A는 먼저 말투를 인정해보세요.",
  safetyLevel: "normal",
};

describe("ResultScreen", () => {
  it("shows verdict, percentages, three reasons, and advice", () => {
    render(<ResultScreen result={result} onRestart={vi.fn()} />);

    expect(screen.getByText("오늘의 판정")).toBeInTheDocument();
    expect(screen.getByText(result.verdict)).toBeInTheDocument();
    expect(screen.getByText("A 68%")).toBeInTheDocument();
    expect(screen.getByText("B 32%")).toBeInTheDocument();
    expect(screen.getAllByRole("listitem")).toHaveLength(3);
    expect(screen.getByText(result.advice)).toBeInTheDocument();
  });

  it("lets the user restart", async () => {
    const onRestart = vi.fn();
    render(<ResultScreen result={result} onRestart={onRestart} />);

    await userEvent.click(screen.getByRole("button", { name: "다시 판정하기" }));

    expect(onRestart).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/features/result/ResultScreen.test.tsx
```

Expected: fails because `ResultScreen` does not exist.

- [ ] **Step 3: Create result screen**

Create `src/features/result/ResultScreen.tsx`:

```tsx
import { Button, Top } from "@toss/tds-mobile";
import type { JudgmentResult } from "../analyzer/types";

type ResultScreenProps = {
  result: JudgmentResult;
  onRestart(): void;
};

export function ResultScreen({ result, onRestart }: ResultScreenProps) {
  const isSafetyResult = result.safetyLevel !== "normal";

  return (
    <main className="screen screen--result">
      <Top
        title={<Top.TitleParagraph size={24}>오늘의 판정</Top.TitleParagraph>}
        subtitleBottom={
          <Top.SubtitleParagraph size={16}>
            입력된 내용 기준의 재미용 판정이에요.
          </Top.SubtitleParagraph>
        }
      />

      <section className={isSafetyResult ? "verdict verdict--safety" : "verdict"}>
        <p className="eyebrow">미스터 노우의 결론</p>
        <h1>{result.verdict}</h1>
      </section>

      <section className="percentage-panel" aria-label="잘못 퍼센티지">
        <div>
          <strong>A {result.partyAPercent}%</strong>
          <span style={{ width: `${result.partyAPercent}%` }} />
        </div>
        <div>
          <strong>B {result.partyBPercent}%</strong>
          <span style={{ width: `${result.partyBPercent}%` }} />
        </div>
      </section>

      <section className="result-section">
        <h2>근거 3개</h2>
        <ol>
          {result.reasons.map((reason) => (
            <li key={reason}>{reason}</li>
          ))}
        </ol>
      </section>

      <section className="result-section">
        <h2>한 줄 조언</h2>
        <p>{result.advice}</p>
      </section>

      <p className="fine-print">
        실제 법률, 의료, 심리 판단이 아니며 빠진 맥락이 있으면 결과가 달라질 수 있어요.
      </p>

      <Button onClick={onRestart}>다시 판정하기</Button>
    </main>
  );
}
```

- [ ] **Step 4: Wire result screen in `src/App.tsx`**

Replace the final `return` block in `src/App.tsx` with:

```tsx
  return <ResultScreen result={screen.result} onRestart={() => setScreen({ name: "home" })} />;
```

Add this import:

```ts
import { ResultScreen } from "./features/result/ResultScreen";
```

- [ ] **Step 5: Add result styling**

Append to `src/App.css`:

```css
.screen--result {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.verdict,
.percentage-panel,
.result-section {
  padding: 16px;
  border: 1px solid #e5e8eb;
  border-radius: 8px;
  background: #ffffff;
}

.verdict h1 {
  margin: 6px 0 0;
  font-size: 24px;
  line-height: 1.25;
}

.verdict--safety {
  border-color: #ffb8b8;
  background: #fff2f2;
}

.percentage-panel {
  display: grid;
  gap: 14px;
}

.percentage-panel div {
  display: grid;
  gap: 8px;
}

.percentage-panel strong {
  font-size: 18px;
}

.percentage-panel span {
  display: block;
  height: 12px;
  min-width: 12px;
  border-radius: 999px;
  background: #3182f6;
}

.percentage-panel div:nth-child(2) span {
  background: #f04452;
}

.result-section h2 {
  margin: 0 0 10px;
  font-size: 18px;
}

.result-section ol {
  display: grid;
  gap: 8px;
  margin: 0;
  padding-left: 20px;
}

.result-section p,
.result-section li {
  color: #333d4b;
  line-height: 1.5;
}

.fine-print {
  margin: 0;
  color: #8b95a1;
  font-size: 12px;
  line-height: 1.45;
}
```

- [ ] **Step 6: Run tests and lint**

Run:

```bash
npm test -- src/features/result/ResultScreen.test.tsx src/features/analyzer/ruleBasedAnalyzer.test.ts
npm run lint
```

Expected: both commands pass.

- [ ] **Step 7: Commit result screen**

```bash
git add src/App.tsx src/App.css src/features/result
git commit -m "feat: add judgment result screen"
```

---

### Task 6: Add OCR Adapter and Screenshot Flow

**Files:**
- Create: `src/features/ocr/ocrAdapter.ts`
- Create: `src/features/ocr/ocrAdapter.test.ts`
- Modify: `src/App.tsx`
- Modify: `src/features/input/TextReview.tsx`
- Modify: `src/App.css`

- [ ] **Step 1: Write OCR adapter tests**

Create `src/features/ocr/ocrAdapter.test.ts`:

```ts
import { describe, expect, it, vi } from "vitest";
import { extractTextFromImage, ocrFailureMessage } from "./ocrAdapter";

describe("ocrAdapter", () => {
  it("returns a friendly failure message for empty files", async () => {
    const file = new File([], "empty.png", { type: "image/png" });

    await expect(extractTextFromImage(file)).rejects.toThrow(ocrFailureMessage);
  });

  it("uses the provided recognizer in tests", async () => {
    const file = new File(["image-bytes"], "chat.png", { type: "image/png" });
    const recognizer = vi.fn().mockResolvedValue("A: 미안해\nB: 네 탓이야");

    await expect(extractTextFromImage(file, recognizer)).resolves.toBe("A: 미안해\nB: 네 탓이야");
    expect(recognizer).toHaveBeenCalledWith(file);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/features/ocr/ocrAdapter.test.ts
```

Expected: fails because `ocrAdapter` does not exist.

- [ ] **Step 3: Create OCR adapter**

Create `src/features/ocr/ocrAdapter.ts`:

```ts
export const ocrFailureMessage = "캡처에서 글자를 읽지 못했어요. 내용을 직접 입력해주세요.";

type Recognizer = (file: File) => Promise<string>;

async function tesseractRecognizer(file: File) {
  const { createWorker } = await import("tesseract.js");
  const worker = await createWorker("kor+eng");

  try {
    const result = await worker.recognize(file);
    return result.data.text.trim();
  } finally {
    await worker.terminate();
  }
}

export async function extractTextFromImage(file: File, recognizer: Recognizer = tesseractRecognizer) {
  if (file.size === 0) {
    throw new Error(ocrFailureMessage);
  }

  const text = (await recognizer(file)).trim();

  if (text.length === 0) {
    throw new Error(ocrFailureMessage);
  }

  return text;
}
```

- [ ] **Step 4: Add optional media controls to `TextReview`**

Extend `TextReviewProps` in `src/features/input/TextReview.tsx`:

```ts
  mediaControl?: React.ReactNode;
```

Render it between the notice and text label:

```tsx
      {mediaControl ? <div className="media-control">{mediaControl}</div> : null}
```

Add React type import:

```ts
import type { ReactNode } from "react";
```

Use `mediaControl?: ReactNode;` in props.

- [ ] **Step 5: Wire screenshot file input in `src/App.tsx`**

Add imports:

```ts
import { extractTextFromImage, ocrFailureMessage } from "./features/ocr/ocrAdapter";
```

Extend the review screen state:

```ts
  | {
      name: "review";
      method: InputMethod;
      initialText: string;
      helperText?: string;
      extractedText?: string;
      extractionError?: string;
    }
```

Add this function inside `App`:

```ts
  async function handleScreenshotFile(file: File) {
    if (screen.name !== "review" || screen.method !== "screenshot") {
      return;
    }

    setScreen({ ...screen, helperText: "캡처에서 글자를 읽는 중이에요.", extractionError: undefined });

    try {
      const extractedText = await extractTextFromImage(file);
      setScreen({
        ...screen,
        initialText: extractedText,
        extractedText,
        helperText: "자동 추출한 글자를 확인하고 이상한 부분만 고쳐주세요.",
      });
    } catch {
      setScreen({
        ...screen,
        extractionError: ocrFailureMessage,
        helperText: ocrFailureMessage,
      });
    }
  }
```

Pass `mediaControl` to `TextReview` when `screen.method === "screenshot"`:

```tsx
        mediaControl={
          screen.method === "screenshot" ? (
            <label className="file-picker">
              <span>캡처 이미지 선택</span>
              <input
                type="file"
                accept="image/*"
                onChange={(event) => {
                  const file = event.currentTarget.files?.[0];
                  if (file) {
                    void handleScreenshotFile(file);
                  }
                }}
              />
            </label>
          ) : undefined
        }
```

- [ ] **Step 6: Add media control styling**

Append to `src/App.css`:

```css
.media-control {
  display: grid;
  gap: 10px;
}

.file-picker {
  display: grid;
  gap: 8px;
  padding: 14px;
  border: 1px dashed #8b95a1;
  border-radius: 8px;
  background: #ffffff;
  color: #333d4b;
  font-size: 14px;
  font-weight: 700;
}

.file-picker input {
  color: #4e5968;
}
```

- [ ] **Step 7: Run tests and lint**

Run:

```bash
npm test -- src/features/ocr/ocrAdapter.test.ts src/features/input/TextReview.test.tsx
npm run lint
```

Expected: both commands pass.

- [ ] **Step 8: Commit OCR flow**

```bash
git add src/App.tsx src/App.css src/features/input/TextReview.tsx src/features/ocr
git commit -m "feat: add screenshot OCR flow"
```

---

### Task 7: Add Audio Recording and Audio File Manual Transcription Flow

**Files:**
- Create: `src/features/audio/audioAdapter.ts`
- Create: `src/features/audio/audioAdapter.test.ts`
- Modify: `src/App.tsx`
- Modify: `src/App.css`

- [ ] **Step 1: Write audio adapter tests**

Create `src/features/audio/audioAdapter.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { createManualTranscriptionMessage, manualTranscriptionAdapter } from "./audioAdapter";

describe("audioAdapter", () => {
  it("returns a manual transcription message for recording", async () => {
    await expect(manualTranscriptionAdapter.transcribe({ source: "recording" })).resolves.toContain("녹음");
  });

  it("returns a manual transcription message for audio files", async () => {
    const file = new File(["audio"], "fight.m4a", { type: "audio/mp4" });

    await expect(manualTranscriptionAdapter.transcribe({ source: "file", file })).resolves.toContain("fight.m4a");
  });

  it("creates concise helper text", () => {
    expect(createManualTranscriptionMessage("recording")).toContain("직접");
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
npm test -- src/features/audio/audioAdapter.test.ts
```

Expected: fails because `audioAdapter` does not exist.

- [ ] **Step 3: Create audio adapter**

Create `src/features/audio/audioAdapter.ts`:

```ts
type TranscriptionInput =
  | { source: "recording"; blob?: Blob }
  | { source: "file"; file: File };

export type TranscriptionAdapter = {
  transcribe(input: TranscriptionInput): Promise<string>;
};

export function createManualTranscriptionMessage(source: TranscriptionInput["source"], fileName?: string) {
  if (source === "file") {
    return `${fileName ?? "녹음 파일"}을 불러왔어요. 들은 내용을 아래에 직접 적거나 붙여넣어 주세요.`;
  }

  return "녹음 흐름을 시작했어요. 분석 전에 들은 내용을 아래에 직접 적거나 붙여넣어 주세요.";
}

export const manualTranscriptionAdapter: TranscriptionAdapter = {
  async transcribe(input) {
    return createManualTranscriptionMessage(input.source, input.source === "file" ? input.file.name : undefined);
  },
};
```

- [ ] **Step 4: Wire audio controls in `src/App.tsx`**

Add import:

```ts
import { manualTranscriptionAdapter } from "./features/audio/audioAdapter";
```

Add functions inside `App`:

```ts
  async function handleRecordingStart() {
    if (screen.name !== "review" || screen.method !== "record") {
      return;
    }

    const helperText = await manualTranscriptionAdapter.transcribe({ source: "recording" });
    setScreen({ ...screen, helperText });
  }

  async function handleAudioFile(file: File) {
    if (screen.name !== "review" || screen.method !== "audio-file") {
      return;
    }

    const helperText = await manualTranscriptionAdapter.transcribe({ source: "file", file });
    setScreen({ ...screen, helperText });
  }
```

Extend `mediaControl` prop in the `TextReview` render:

```tsx
        mediaControl={
          screen.method === "screenshot" ? (
            <label className="file-picker">
              <span>캡처 이미지 선택</span>
              <input
                type="file"
                accept="image/*"
                onChange={(event) => {
                  const file = event.currentTarget.files?.[0];
                  if (file) {
                    void handleScreenshotFile(file);
                  }
                }}
              />
            </label>
          ) : screen.method === "record" ? (
            <button className="media-button" type="button" onClick={() => void handleRecordingStart()}>
              녹음 흐름 시작하기
            </button>
          ) : screen.method === "audio-file" ? (
            <label className="file-picker">
              <span>녹음 파일 선택</span>
              <input
                type="file"
                accept="audio/*"
                onChange={(event) => {
                  const file = event.currentTarget.files?.[0];
                  if (file) {
                    void handleAudioFile(file);
                  }
                }}
              />
            </label>
          ) : undefined
        }
```

- [ ] **Step 5: Add audio button styling**

Append to `src/App.css`:

```css
.media-button {
  width: 100%;
  min-height: 48px;
  border: 0;
  border-radius: 8px;
  background: #191f28;
  color: #ffffff;
  font-size: 16px;
  font-weight: 800;
}
```

- [ ] **Step 6: Run tests and lint**

Run:

```bash
npm test -- src/features/audio/audioAdapter.test.ts src/features/input/TextReview.test.tsx
npm run lint
```

Expected: both commands pass.

- [ ] **Step 7: Commit audio flow**

```bash
git add src/App.tsx src/App.css src/features/audio
git commit -m "feat: add audio input flow"
```

---

### Task 8: Add README Verification and Privacy Notes

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace README content**

Replace `README.md` with this content:

````md
# 미스터 노우

Toss Apps in Toss WebView 미니앱 MVP입니다. 사용자가 카톡 대화, 캡처, 녹음 흐름, 녹음 파일을 제출하면 미스터 노우가 입력된 내용 기준으로 잘못 비율과 근거를 판정합니다.

## 실행

```bash
npm install
npm run dev
```

## 검증

```bash
npm run lint
npm test
npm run build
```

현재 `@apps-in-toss/ait-format@1.0.0`은 Node `>=24`를 요구합니다. 로컬 Node가 낮으면 `npm run build`에서 engine 관련 실패가 날 수 있습니다. 이 경우 Node 24 이상으로 다시 확인합니다.

## 개인정보 원칙

- MVP는 입력 텍스트, 이미지, 음성 파일을 서버에 저장하지 않습니다.
- 캡처 OCR은 브라우저 내 처리를 기본으로 합니다.
- 녹음과 녹음 파일은 분석 전 사용자가 텍스트를 확인하거나 직접 입력합니다.
- 결과는 법률, 의료, 심리 판단이 아닌 재미용 판정입니다.
````

- [ ] **Step 2: Run formatter**

Run:

```bash
npm run format
```

Expected: README and code formatting completes.

- [ ] **Step 3: Commit README**

```bash
git add README.md
git commit -m "docs: add Mr Know setup notes"
```

---

### Task 9: Full Verification and gstack Review Prep

**Files:**
- Modify only if verification reveals issues.

- [ ] **Step 1: Run full tests**

Run:

```bash
npm test
```

Expected: all test files pass.

- [ ] **Step 2: Run lint**

Run:

```bash
npm run lint
```

Expected: passes with no errors.

- [ ] **Step 3: Run production build**

Run:

```bash
npm run build
```

Expected: passes on Node 24+. If local Node 20 fails with an engine error from `@apps-in-toss/ait-format`, record the exact output and verify again after switching Node to `>=24`.

- [ ] **Step 4: Start dev server**

Run:

```bash
npm run dev
```

Expected: Granite dev server starts and prints a local URL.

- [ ] **Step 5: Manually verify the four product flows**

In the local app, verify:

- `카톡 싸움 붙여넣기` opens text review with sample text and reaches result.
- `증거 캡처 제출하기` accepts an image, attempts OCR, and reaches editable text review.
- `현장 녹음 시작` shows the recording/manual text helper and reaches result after typing text.
- `녹음 파일 불러오기` accepts an audio file, shows the manual text helper, and reaches result after typing text.

- [ ] **Step 6: Prepare gstack review prompt**

Run gstack against the repository with this review goal:

```text
Review this Toss Apps in Toss mini app MVP for structure, testability, mobile UX, privacy-sensitive media handling, and whether the four required flows match the design doc at docs/superpowers/specs/2026-05-20-mr-know-design.md. Focus on blocking bugs, missing tests, and Toss WebView compatibility risks.
```

Expected: review output has no blocking issues. If blocking issues appear, fix them in a new task and rerun tests.

- [ ] **Step 7: Commit final fixes if any**

If files changed during verification:

```bash
git add .
git commit -m "fix: address verification findings"
```

If no files changed, do not create an empty commit.

---

## Self-Review

Spec coverage:

- Toss WebView scaffold: Task 1.
- Four input cards with approved labels: Task 3.
- Text confirmation before analysis: Task 4, Task 6, Task 7.
- Rule-based analyzer and LLM-compatible interface: Task 2 defines the pure analyzer interface and default implementation.
- OCR automatic extraction: Task 6.
- Audio recording/file flow with manual text confirmation MVP: Task 7.
- Result screen with verdict, percentages, three reasons, and advice: Task 5.
- Safety detection and non-diagnostic wording: Task 2 and Task 5.
- Tests and gstack verification prep: Task 9.

Placeholder scan:

- The plan contains no placeholder markers or missing implementation steps.
- Follow-up functionality is explicitly scoped out as non-MVP where relevant.

Type consistency:

- `JudgmentResult`, `AnalyzeInput`, `Analyzer`, `SafetyLevel`, `InputMethod`, and `TranscriptionAdapter` are defined before use.
- UI tasks reference only components or functions created in earlier tasks or in the same task.
